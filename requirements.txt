# --- Core ---
python-dotenv>=1.0.0       # carga el .env como variables de entorno

# --- LLM y STT ---
openai>=1.50.0              # cliente de OpenAI (GPT + Whisper)

# --- Base de datos ---
supabase>=2.8.0             # cliente de Supabase (PostgreSQL + Storage + Auth)
postgrest>=0.16.0           # cliente HTTP a PostgREST (usado internamente)

# --- Servidor web (para el webhook de WhatsApp) ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0   # servidor ASGI
gunicorn>=23.0.0            # servidor de producción (Cloud Run)

# --- HTTP y validación ---
httpx>=0.27.0               # cliente HTTP async (para llamar a Meta)
pydantic>=2.9.0             # validación de datos

# --- Utilidades ---
python-multipart>=0.0.12    # para parsear multipart (Meta envía cosas así)
